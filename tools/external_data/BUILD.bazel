load("//tools/lint:lint.bzl", "add_lint_tests")

sh_test(
    name = "workspace_test",
    srcs = ["workspace_test.sh"],
    args = ["$(location .)"],
    data = [
        # This directory is already containd by this group.
        "//tools:external_data_deps",
    ],
)

add_lint_tests()
