# -*- python -*-

load("//tools/lint:lint.bzl", "add_lint_tests")

package(default_visibility = ["//visibility:public"])

licenses(["permissive"])

py_library(
    name = "wrapt",
    srcs = [
        "__init__.py",
        "wrappers.py",
    ],
    imports = [".."],
    tags = ["nolint"],
)

# TODO(eric.cousineau): Install this once it's used in `pydrake`.

add_lint_tests()
